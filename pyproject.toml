[project]
name = "buckshot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cashton Nadar", email = "cashtonnadar11@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
buckshot = "buckshot:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
